# VectorEmbedSiYuan

This is a bounded local utility. Read README.md and config.json first.
Source roots are read-only: E:\.siyuan and canonical-reference D:\.city_hall.
Never directly write `.sy` files or native SiYuan database/vector tables.
Use supported SiYuan APIs for user-authorized settings or note changes.
Preserve existing providers, secrets, MCP servers, and unrelated settings.
The external catalog contains private source text; keep it local and gitignored.
Keep source/block provenance and City Planning's non-governing boundary visible.
Run one inventory/embed writer at a time. Preserve completed embedding batches.
Run the unittest suite after extractor, invalidation, or retrieval changes.
Document integration changes and distinguish tool availability from Agent smoke.
